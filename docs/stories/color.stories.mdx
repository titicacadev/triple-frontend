import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import { select as selectAddons, text, boolean } from '@storybook/addon-knobs'
import styled from 'styled-components'
import { light, dark } from '@titicaca/theme'

<Meta title="Theme" />

# Colors

<br />

## COLORS - LIGHT

<Preview>
  <Story name="colors - light">
    {Object.keys(light).map((key, idx) => (
      <div key={idx} style={{ position: 'relative', margin: '0 0 10px 0'}}>
        <div
          style={{
            width: '100%',
            height: '50px',
            background: light[key],
            border: `1px solid ${light.gray_10}`,
          }}
        />
        <span style={{
          color: light.white,
          position: 'absolute',
          top: '50%',
          transform: 'translateY(-50%)',
          left: '5px',
          textShadow: `0px 1px 5px ${light.gray_80}`,
          fontWeight: 'bold'
        }}>
          {key}: {light[key]}
        </span> 
      </div>
    ))}
  </Story>
</Preview>
