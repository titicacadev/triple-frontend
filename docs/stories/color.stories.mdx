import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import { select as selectAddons, text, boolean } from '@storybook/addon-knobs'
import styled from 'styled-components'
import { light, dark } from '@titicaca/theme'

<Meta title="Theme" />

# Colors

<br />

## COLORS - LIGHT

<Preview>
  <Story name="light-2">
    {Object.keys(light.color).map((key, idx) => (
      <div key={idx}>
        <div
          style={{
            width: '100%',
            height: '50px',
            background: `rgba(${light.color[key]})`,
            border: '1px solid #efefef',
          }}
        />
        name: {key}
        value: {`rgba(${light.color[key]})`}
      </div>
    ))}
  </Story>
</Preview>
