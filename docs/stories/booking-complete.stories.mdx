import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import { select, text, boolean } from '@storybook/addon-knobs'
import BookingComplete from '@titicaca/booking-complete'

<Meta title="Booking Complete" />

# Booking Complete

<BookingComplete
  detailLink={boolean('내 예약에서 확인', true)}
  listLink={boolean('다른 상품 더보기', true)}
  descriptions={[
    '공급사 확인 후 예약이 확정됩니다.',
    '예약이 확정되면 이메일로 바우처가 발송됩니다.',
  ]}
/>
