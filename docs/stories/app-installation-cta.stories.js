import React from 'react'
import { storiesOf } from '@storybook/react'

import AppInstallationCTA from '@titicaca/app-installation-cta'

storiesOf('AppInstallationCTA', module).add('기본', () => (
  <AppInstallationCTA />
))
