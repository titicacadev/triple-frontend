import React from 'react'

import { storiesOf } from '@storybook/react'

import { PublicHeader } from '@titicaca/triple-design-system'

storiesOf('PublicHeader', module).add('일반', () => <PublicHeader />)
