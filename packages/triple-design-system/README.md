# Triple Design System

**WARNING** USE WITH CARE. Highly sketchy/volatile implementation.

## See it in action

Build and run `triple-article` example app:

```
$ cd examples/triple-article
$ npm install
$ npm start
# Open http://localhost:8080
```
