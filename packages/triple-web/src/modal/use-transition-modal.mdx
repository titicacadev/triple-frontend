import { Meta, ArgTypes } from '@storybook/blocks'

<Meta title="triple-web / useTransitionModal" />

# useTransitionModal

## Usage

```tsx
const { show, close } = useTransitionModal()
```

## Returns

### show

`(transitionType: TransitionType, options?: ShowOptions) => void`

### close

`() => void`
