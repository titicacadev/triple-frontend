name: Status Update
on: pull_request
jobs:
  status-update:
    runs-on: ubuntu-latest
    steps:
    - uses: 'jessfraz/shaking-finger-action@master'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
